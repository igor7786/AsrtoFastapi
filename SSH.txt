#GITHUB
ssh-keygen -o -t rsa -C "grimuta60@gmail.com"
then press enter
then phase
then github.com -> profile -> ssh keys -> add ssh key
coopy public key from file ~/.ssh/id_rsa.pub and paste in github
add tittle
then confirm
after code -> ssh -> git clone -> git@github.com:igor7786/AsrtoFastapi.git

#REMOTE-SERVER
ssh username@hostname_or_ip
enter password
exit
ssh-keygen -t ed25519 -C "example@domain.com" -f ~/.ssh/id_somthing
Enter passphrase (empty for no passphrase):
👉 Option A (Recommended for security): Type a strong password. (Note: Nothing will show on screen while typing).
👉 Option B (Easiest for automation/scripts): Press Enter twice to leave it empty.
#Try to connect
ssh -i ~/.ssh/id_somthing root@hostname_or_ip
exit
#On main PC
sudo nano ~/.ssh/config
Host something
    HostName 185.230.218.3
    User root
    IdentityFile ~/.ssh/id_something
    IdentitiesOnly yes
now try to connect
ssh something

#Key already exsist
# On your PC:
chmod 600 "/home/igor7786/Downloads/ssh-key-oracle.key"
mkdir -p ~/.ssh
mv "/home/igor7786/Downloads/ssh-key-oracle.key" ~/.ssh/ssh-key-oracle.key
chmod 600 ~/.ssh/ssh-key-oracle.key
chmod 700 ~/.ssh

ssh -i ~/.ssh/ssh-key-oracle.key ubuntu@84.8.155.57


# On the server (as ubuntu):
r
# Check if ssh keys exists
ls -ld /home/ubuntu/.ssh
ls -l /home/ubuntu/.ssh/authorized_keys

sudo adduser igor
sudo usermod -aG sudo igor

# If you wanna create new user
sudo mkdir -p /home/igor/.ssh
sudo cp /home/root/.ssh/authorized_keys /home/igor/.ssh/
sudo chown -R igor:igor /home/igor/.ssh
sudo chmod 700 /home/igor/.ssh
sudo chmod 600 /home/igor/.ssh/authorized_keys

# Back on your PC (test):
ssh -i ~/.ssh/ssh-key-oracle.key ubuntu@84.8.155.57

# Only after that succeeds, on the server:
sudo nano /etc/ssh/sshd_config
# add at bottom:
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes
KbdInteractiveAuthentication no
UsePAM yes

sudo sshd -t
sudo systemctl reload ssh

# Optional after confirming everything:
sudo passwd -l ubuntu
# and/or
# AllowUsers igor  (optional)
sudo sshd -t
sudo systemctl reload ssh


